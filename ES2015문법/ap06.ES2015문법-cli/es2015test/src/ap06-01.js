'use strict';

const name = 'world';
console.log(`Hello ${name}`);

const getMessage = (params) => {
  console.log(`Hello ${params}`);
};
getMessage('sejoong');

const str = 'aaaa';
console.log(str);
